import { PaymentCardDirective } from './payment-card.directive';

describe('PaymentCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PaymentCardDirective();
    expect(directive).toBeTruthy();
  });
});
